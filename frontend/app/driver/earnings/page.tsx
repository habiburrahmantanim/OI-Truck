"use client";
import { Banknote, CalendarDays, CircleDollarSign, TrendingUp } from "lucide-react";
import { useBooking } from "@/context/BookingContext";
export default function EarningsPage() { const { bookings, isLoaded } = useBooking(); const trips = bookings.filter(b => b.status === "Delivered"); const total = trips.reduce((sum,b) => sum + Math.round(b.estimatedFare*.78),0); const week = Math.round(total*.38); return <div className="mx-auto max-w-6xl"><p className="text-sm font-semibold text-orange-600">PAYMENTS</p><h1 className="mt-1 text-2xl font-bold text-slate-900 sm:text-3xl">Earnings</h1><p className="mt-1 text-sm text-slate-500">Your delivery income after the platform service fee.</p><section className="mt-7 grid gap-4 sm:grid-cols-3"><Card icon={CircleDollarSign} label="Total earnings" value={isLoaded ? `BDT ${total.toLocaleString()}` : "..."} /><Card icon={TrendingUp} label="This week" value={`BDT ${week.toLocaleString()}`} /><Card icon={CalendarDays} label="Completed trips" value={trips.length.toString()} /></section><section className="mt-7 overflow-hidden rounded-lg border border-slate-200 bg-white"><div className="border-b border-slate-100 p-5"><h2 className="font-bold text-slate-900">Trip payouts</h2><p className="mt-1 text-sm text-slate-500">78% of each completed fare is credited to your balance.</p></div><div className="overflow-x-auto"><table className="w-full min-w-160 text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Booking</th><th className="px-5 py-3">Trip fare</th><th className="px-5 py-3">Service fee</th><th className="px-5 py-3">Your payout</th><th className="px-5 py-3">Status</th></tr></thead><tbody>{trips.length ? trips.map(b => { const payout=Math.round(b.estimatedFare*.78); return <tr key={b.id} className="border-t border-slate-100"><td className="px-5 py-4 font-semibold text-slate-800">{b.bookingId}</td><td className="px-5 py-4 text-slate-600">BDT {b.estimatedFare.toLocaleString()}</td><td className="px-5 py-4 text-slate-600">BDT {(b.estimatedFare-payout).toLocaleString()}</td><td className="px-5 py-4 font-semibold text-green-700">BDT {payout.toLocaleString()}</td><td className="px-5 py-4"><span className="rounded-full bg-green-100 px-2.5 py-1 text-xs font-semibold text-green-700">Paid</span></td></tr> }) : <tr><td colSpan={5} className="px-5 py-12 text-center text-slate-500">No completed trips available for payout.</td></tr>}</tbody></table></div></section></div>; }
function Card({icon:Icon,label,value}:{icon:typeof Banknote;label:string;value:string}) { return <div className="rounded-lg border border-slate-200 bg-white p-5"><span className="inline-flex rounded-lg bg-orange-100 p-2.5 text-orange-600"><Icon size={21}/></span><p className="mt-4 text-sm text-slate-500">{label}</p><p className="mt-1 text-2xl font-bold text-slate-900">{value}</p></div>; }

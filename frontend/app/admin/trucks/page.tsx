"use client";

import { Edit3, Plus, Search, Truck, Wrench } from "lucide-react";
import { useMemo, useState } from "react";
import { trucks } from "@/data/data";

type FleetState = "Available" | "On delivery" | "Maintenance";
const initialStatus: Record<number, FleetState> = { 1: "Available", 2: "On delivery", 3: "Available", 4: "Maintenance", 5: "On delivery", 6: "Available", 7: "Available" };

export default function AdminTrucksPage() {
  const [query, setQuery] = useState(""); const [status, setStatus] = useState(initialStatus); const [notice, setNotice] = useState("");
  const filtered = useMemo(() => trucks.filter(t => `${t.name} ${t.capacity} ${t.category}`.toLowerCase().includes(query.toLowerCase())), [query]);
  function update(id: number, state: FleetState) { setStatus((current) => ({ ...current, [id]: state })); setNotice("Fleet status updated."); window.setTimeout(() => setNotice(""), 2200); }
  return <div className="mx-auto max-w-7xl"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold text-orange-600">FLEET</p><h1 className="mt-1 text-2xl font-bold text-slate-900 sm:text-3xl">Manage trucks</h1><p className="mt-1 text-sm text-slate-500">Keep vehicle availability and maintenance status current.</p></div><button type="button" onClick={() => setNotice("New truck form will be available when fleet persistence is connected.")} className="inline-flex w-fit items-center gap-2 rounded-md bg-orange-500 px-4 py-2.5 text-sm font-semibold text-white hover:bg-orange-600"><Plus size={18} />Add truck</button></div>{notice && <p className="mt-4 rounded-md bg-green-50 px-4 py-3 text-sm font-medium text-green-700">{notice}</p>}<div className="relative mt-7 max-w-md"><Search size={18} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search fleet" className="w-full rounded-md border border-slate-200 bg-white py-2.5 pl-10 pr-3 text-sm outline-none focus:border-orange-500" /></div><section className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{filtered.map(truck => <article key={truck.id} className="overflow-hidden rounded-lg border border-slate-200 bg-white"><div className="flex items-start justify-between p-5"><span className="flex h-11 w-11 items-center justify-center rounded-lg bg-orange-100 text-orange-600"><Truck size={22} /></span><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${status[truck.id] === "Available" ? "bg-green-100 text-green-700" : status[truck.id] === "On delivery" ? "bg-blue-100 text-blue-700" : "bg-amber-100 text-amber-700"}`}>{status[truck.id]}</span></div><div className="px-5"><h2 className="font-bold text-slate-900">{truck.name}</h2><p className="mt-1 text-sm text-slate-500">{truck.category} vehicle · {truck.capacity} capacity</p><div className="mt-5 grid grid-cols-2 border-t border-slate-100 pt-4 text-sm"><div><p className="text-xs text-slate-400">Starting fare</p><p className="mt-1 font-semibold text-slate-800">BDT {truck.price.toLocaleString()}</p></div><div><p className="text-xs text-slate-400">Service type</p><p className="mt-1 font-semibold text-slate-800">Standard</p></div></div></div><div className="mt-5 flex border-t border-slate-100"><label className="flex flex-1 items-center gap-2 px-5 py-3 text-sm font-semibold text-slate-600"><Wrench size={16} /><select aria-label={`Fleet status for ${truck.name}`} value={status[truck.id]} onChange={(e) => update(truck.id, e.target.value as FleetState)} className="w-full bg-transparent outline-none"><option>Available</option><option>On delivery</option><option>Maintenance</option></select></label><button type="button" onClick={() => setNotice(`${truck.name} details are ready to edit when fleet persistence is connected.`)} className="border-l border-slate-100 px-4 text-slate-500 hover:bg-slate-50 hover:text-orange-600" aria-label={`Edit ${truck.name}`}><Edit3 size={17} /></button></div></article>)}</section></div>;
}

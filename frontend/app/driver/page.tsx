"use client";
import Link from "next/link";
import { ArrowRight, CalendarDays, CircleDollarSign, Clock3, Route, Truck } from "lucide-react";
import { useBooking } from "@/context/BookingContext";
export default function DriverDashboard() { const { bookings, isLoaded } = useBooking(); const active = bookings.filter(b => ["Driver Assigned", "Picked Up", "In Transit"].includes(b.status)); const completed = bookings.filter(b => b.status === "Delivered"); const earned = completed.reduce((sum, b) => sum + Math.round(b.estimatedFare * .78), 0); return <div className="mx-auto max-w-7xl"><p className="text-sm font-semibold text-orange-600">GOOD MORNING, HASAN</p><h1 className="mt-1 text-2xl font-bold text-slate-900 sm:text-3xl">Your driver dashboard</h1><p className="mt-1 text-sm text-slate-500">Here is your delivery activity for today.</p><section className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={Truck} label="Active assignments" value={isLoaded ? active.length : "..."} /><Metric icon={CalendarDays} label="Completed trips" value={completed.length} /><Metric icon={CircleDollarSign} label="Total earnings" value={`BDT ${earned.toLocaleString()}`} /><Metric icon={Clock3} label="Availability" value="Available" /></section><section className="mt-7 grid gap-6 xl:grid-cols-[1.65fr_1fr]"><div className="rounded-lg border border-slate-200 bg-white"><div className="flex items-center justify-between border-b border-slate-100 p-5"><div><h2 className="font-bold text-slate-900">Current assignments</h2><p className="mt-1 text-sm text-slate-500">Deliveries that need your attention.</p></div><Link href="/driver/assignments" className="text-sm font-semibold text-orange-600">View all</Link></div>{active.length ? <div>{active.slice(0, 3).map(b => <div key={b.id} className="flex flex-col gap-4 border-b border-slate-100 p-5 last:border-0 sm:flex-row sm:items-center sm:justify-between"><div className="flex gap-3"><span className="mt-1 text-orange-500"><Route size={20} /></span><div><p className="font-semibold text-slate-800">{b.pickupLocation} to {b.dropLocation}</p><p className="mt-1 text-sm text-slate-500">{b.bookingId} · {b.truckName}</p></div></div><Link href="/driver/assignments" className="inline-flex items-center gap-1 text-sm font-semibold text-orange-600">Open <ArrowRight size={16} /></Link></div>)}</div> : <div className="p-10 text-center text-sm text-slate-500">No active assignments. New work will appear here.</div>}</div><div className="rounded-lg bg-slate-900 p-6 text-white"><p className="text-sm font-semibold text-orange-400">YOUR VEHICLE</p><h2 className="mt-3 text-xl font-bold">Pickup Truck</h2><p className="mt-1 text-sm text-slate-400">DHAKA METRO-TA 13-4521</p><div className="mt-7 space-y-4 border-t border-white/10 pt-5 text-sm"><Line label="Capacity" value="1 Ton" /><Line label="Fuel status" value="78%" /><Line label="Next service" value="28 Aug 2026" /></div><Link href="/driver/profile" className="mt-6 inline-flex items-center gap-1 text-sm font-semibold text-orange-400">View vehicle details <ArrowRight size={16} /></Link></div></section></div>; }
function Metric({ icon: Icon, label, value }: { icon: typeof Truck; label: string; value: string | number }) { return <div className="rounded-lg border border-slate-200 bg-white p-5"><div className="flex items-start justify-between"><div><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold text-slate-900">{value}</p></div><span className="rounded-lg bg-orange-100 p-2.5 text-orange-600"><Icon size={21} /></span></div></div>; } function Line({label,value}:{label:string;value:string}) { return <div className="flex justify-between"><span className="text-slate-400">{label}</span><span className="font-semibold">{value}</span></div>; }

"use client";
import { CalendarDays, MapPin, Route, Search } from "lucide-react";
import { useMemo, useState } from "react";
import { useBooking } from "@/context/BookingContext";
export default function TripsPage() { const { bookings, isLoaded } = useBooking(); const [query,setQuery] = useState(""); const trips = useMemo(() => bookings.filter(b => b.status === "Delivered" && `${b.bookingId} ${b.pickupLocation} ${b.dropLocation}`.toLowerCase().includes(query.toLowerCase())),[bookings,query]); return <div className="mx-auto max-w-6xl"><p className="text-sm font-semibold text-orange-600">HISTORY</p><h1 className="mt-1 text-2xl font-bold text-slate-900 sm:text-3xl">Completed trips</h1><p className="mt-1 text-sm text-slate-500">Review your completed delivery history.</p><div className="relative mt-7 max-w-md"><Search size={18} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search trip or location" className="w-full rounded-md border border-slate-200 bg-white py-2.5 pl-10 pr-3 text-sm outline-none focus:border-orange-500" /></div><section className="mt-5 overflow-hidden rounded-lg border border-slate-200 bg-white"><div className="overflow-x-auto"><table className="w-full min-w-180 text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Trip</th><th className="px-5 py-3">Route</th><th className="px-5 py-3">Vehicle</th><th className="px-5 py-3">Completed</th><th className="px-5 py-3">Earnings</th></tr></thead><tbody>{!isLoaded ? <tr><td colSpan={5} className="px-5 py-8 text-slate-500">Loading trips...</td></tr> : trips.length ? trips.map(b => <tr key={b.id} className="border-t border-slate-100"><td className="px-5 py-4 font-semibold text-slate-800">{b.bookingId}</td><td className="px-5 py-4 text-slate-600"><p className="flex items-center gap-1"><MapPin size={14} className="text-orange-500" />{b.pickupLocation}</p><p className="mt-1 flex items-center gap-1 text-xs text-slate-400"><Route size={14} />{b.dropLocation}</p></td><td className="px-5 py-4 text-slate-600">{b.truckName}</td><td className="px-5 py-4 text-slate-600"><span className="flex items-center gap-1"><CalendarDays size={14} />{new Date(b.createdAt).toLocaleDateString("en-BD")}</span></td><td className="px-5 py-4 font-semibold text-green-700">BDT {Math.round(b.estimatedFare*.78).toLocaleString()}</td></tr>) : <tr><td colSpan={5} className="px-5 py-12 text-center text-slate-500">No completed trips yet.</td></tr>}</tbody></table></div></section></div>; }

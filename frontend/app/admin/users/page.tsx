"use client";

import { Mail, Search, UserRound } from "lucide-react";
import { useMemo, useState } from "react";

const users = [{ name: "Ayesha Rahman", email: "ayesha.rahman@example.com", bookings: 8, joined: "12 Aug 2026", state: "Active" }, { name: "Karim Uddin", email: "karim.uddin@example.com", bookings: 5, joined: "10 Aug 2026", state: "Active" }, { name: "Mina Akter", email: "mina.akter@example.com", bookings: 3, joined: "08 Aug 2026", state: "Active" }, { name: "Tania Sultana", email: "tania.sultana@example.com", bookings: 0, joined: "02 Aug 2026", state: "Suspended" }];
export default function AdminUsersPage() { const [query, setQuery] = useState(""); const filtered = useMemo(() => users.filter(u => `${u.name} ${u.email}`.toLowerCase().includes(query.toLowerCase())), [query]); return <div className="mx-auto max-w-7xl"><p className="text-sm font-semibold text-orange-600">CUSTOMERS</p><h1 className="mt-1 text-2xl font-bold text-slate-900 sm:text-3xl">Manage users</h1><p className="mt-1 text-sm text-slate-500">Review customer accounts and their booking activity.</p><section className="mt-7 overflow-hidden rounded-lg border border-slate-200 bg-white"><div className="flex flex-col gap-4 border-b border-slate-100 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="relative w-full max-w-md"><Search size={18} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search customer name or email" className="w-full rounded-md border border-slate-200 py-2.5 pl-10 pr-3 text-sm outline-none focus:border-orange-500" /></div><p className="text-sm text-slate-500">{filtered.length} customer accounts</p></div><div className="overflow-x-auto"><table className="w-full min-w-175 text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Customer</th><th className="px-5 py-3">Bookings</th><th className="px-5 py-3">Joined</th><th className="px-5 py-3">Account status</th></tr></thead><tbody>{filtered.map(u => <tr key={u.email} className="border-t border-slate-100"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-orange-100 text-orange-700"><UserRound size={18} /></span><div><p className="font-semibold text-slate-800">{u.name}</p><p className="mt-0.5 flex items-center gap-1 text-xs text-slate-400"><Mail size={11} />{u.email}</p></div></div></td><td className="px-5 py-4 font-semibold text-slate-700">{u.bookings}</td><td className="px-5 py-4 text-slate-600">{u.joined}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${u.state === "Active" ? "bg-green-100 text-green-700" : "bg-red-100 text-red-700"}`}>{u.state}</span></td></tr>)}</tbody></table></div></section></div>; }

"use client";

import { CheckCircle2, CircleCheck, Phone, Plus, Search, Star, UserRound } from "lucide-react";
import { useMemo, useState } from "react";

type Application = { id: string; name: string; phone: string; email: string; license: string; nid: string; vehicle: string; registration: string; status: "Pending" | "Approved" | "Rejected"; createdAt: string };
const drivers = [{ name: "Hasan Mahmud", phone: "+880 1712-555001", truck: "Pickup Truck", trips: 82, rating: "4.9", status: "Available" }, { name: "Rakib Hossain", phone: "+880 1712-555002", truck: "Mini Covered Van", trips: 64, rating: "4.8", status: "On delivery" }, { name: "Sajid Khan", phone: "+880 1712-555003", truck: "Heavy Duty Truck", trips: 51, rating: "4.9", status: "Available" }, { name: "Imran Ali", phone: "+880 1712-555004", truck: "Covered Truck", trips: 44, rating: "4.7", status: "Off duty" }];

export default function AdminDriversPage() { const [query,setQuery]=useState(""); const [applications,setApplications]=useState<Application[]>(() => { if (typeof window === "undefined") return []; try { return JSON.parse(localStorage.getItem("trucklagbe-driver-applications") || "[]"); } catch { return []; } }); const [notice,setNotice]=useState(""); const filtered=useMemo(() => drivers.filter(d => `${d.name} ${d.truck}`.toLowerCase().includes(query.toLowerCase())),[query]); function approve(id:string) { const updated=applications.map(app => app.id === id ? {...app,status:"Approved" as const} : app); setApplications(updated); localStorage.setItem("trucklagbe-driver-applications",JSON.stringify(updated)); setNotice("Driver application approved. The driver can now access the driver panel."); } return <div className="mx-auto max-w-7xl"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold text-orange-600">PEOPLE</p><h1 className="mt-1 text-2xl font-bold text-slate-900 sm:text-3xl">Manage drivers</h1><p className="mt-1 text-sm text-slate-500">Review applications and track driver availability.</p></div><button type="button" onClick={() => setNotice("Use the driver registration page to submit a new driver application.")} className="inline-flex w-fit items-center gap-2 rounded-md bg-orange-500 px-4 py-2.5 text-sm font-semibold text-white hover:bg-orange-600"><Plus size={18} />Add driver</button></div>{notice && <p className="mt-4 rounded-md bg-green-50 px-4 py-3 text-sm font-medium text-green-700">{notice}</p>}{applications.filter(a=>a.status==="Pending").length > 0 && <section className="mt-7 overflow-hidden rounded-lg border border-amber-200 bg-white"><div className="border-b border-amber-100 bg-amber-50 px-5 py-4"><h2 className="font-bold text-amber-900">Pending driver applications</h2><p className="mt-1 text-sm text-amber-700">Verify submitted identity and vehicle details before approval.</p></div><div className="divide-y divide-slate-100">{applications.filter(a=>a.status==="Pending").map(app=><div key={app.id} className="flex flex-col gap-4 p-5 lg:flex-row lg:items-center lg:justify-between"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div><p className="font-semibold text-slate-800">{app.name}</p><p className="text-sm text-slate-500">{app.phone}</p></div><div><p className="text-xs font-semibold uppercase text-slate-400">Licence / NID</p><p className="mt-1 text-sm text-slate-700">{app.license} · {app.nid}</p></div><div><p className="text-xs font-semibold uppercase text-slate-400">Vehicle</p><p className="mt-1 text-sm text-slate-700">{app.vehicle}</p></div><div><p className="text-xs font-semibold uppercase text-slate-400">Registration</p><p className="mt-1 text-sm text-slate-700">{app.registration}</p></div></div><button type="button" onClick={() => approve(app.id)} className="inline-flex shrink-0 items-center justify-center gap-2 rounded-md bg-green-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-green-700"><CheckCircle2 size={17}/>Approve</button></div>)}</div></section>}<div className="relative mt-7 max-w-md"><Search size={18} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search driver or assigned truck" className="w-full rounded-md border border-slate-200 bg-white py-2.5 pl-10 pr-3 text-sm outline-none focus:border-orange-500"/></div><section className="mt-5 overflow-hidden rounded-lg border border-slate-200 bg-white"><div className="overflow-x-auto"><table className="w-full min-w-195 text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Driver</th><th className="px-5 py-3">Assigned truck</th><th className="px-5 py-3">Completed trips</th><th className="px-5 py-3">Rating</th><th className="px-5 py-3">Availability</th></tr></thead><tbody>{filtered.map(d=><tr key={d.phone} className="border-t border-slate-100"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-slate-100 text-slate-600"><UserRound size={18}/></span><div><p className="font-semibold text-slate-800">{d.name}</p><p className="mt-0.5 flex items-center gap-1 text-xs text-slate-400"><Phone size={11}/>{d.phone}</p></div></div></td><td className="px-5 py-4 text-slate-600">{d.truck}</td><td className="px-5 py-4 font-semibold text-slate-700">{d.trips}</td><td className="px-5 py-4"><span className="flex items-center gap-1 font-semibold text-slate-700"><Star size={15} className="fill-amber-400 text-amber-400"/>{d.rating}</span></td><td className="px-5 py-4"><span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-semibold ${d.status==="Available"?"bg-green-100 text-green-700":d.status==="On delivery"?"bg-blue-100 text-blue-700":"bg-slate-100 text-slate-600"}`}><CircleCheck size={13}/>{d.status}</span></td></tr>)}</tbody></table></div></section></div>; }
